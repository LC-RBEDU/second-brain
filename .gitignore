# macOS Finder (custom folder icon metadata — breaks git if it lands in .git/)
Icon?
.DS_Store

# Cursor IDE local state (not project source)
.cursor/

# Vault content (SSoT = Google Drive, ne git)
# - úkoly, projekty, INBOX, dashboard JSON/HTML, weekly drafts, retro, Memory, …
# - vše se synchronizuje přes Google Drive Desktop mirror, repo drží jen kód
OBSIDIAN/

# Generated outputs / Python cache
vps/second-brain-hub/web/dashboard-data.json
**/__pycache__/
*.pyc

# Python venvs (heavy, regenerate from requirements.txt)
.venv/
venv/

# Test artefacts
.pytest_cache/

# Secrets (never commit)
*.client_secret*.json
oauth_creds.json
oauth_client.json
