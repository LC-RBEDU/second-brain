# macOS Finder (custom folder icon metadata — breaks git if it lands in .git/)
Icon?
.DS_Store

# Cursor IDE local state (not project source)
.cursor/

# Generated dashboard / Python cache (SSOT is MrLUC vault on iCloud)
vps/second-brain-hub/web/dashboard-data.json
**/__pycache__/
*.pyc
